    function sum(arr,n)
            {
                //console.log(arr);
                //console.log(n);
                if (n <= 0) {
                  return arr[0]; } 
                return sum(arr, n - 1) + arr[n];
            
            }
